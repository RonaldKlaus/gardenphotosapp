%table
  %tr
    %th Rolle
    %th Beschreibung
  - @roles.each do |role|
    %tr.user
      %td.role-name
        = role.name
      %td.role-description
        = role.description
      %td.buttons

